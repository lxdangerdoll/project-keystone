# Environment Configuration Template
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (when migrating from in-memory storage)
DATABASE_URL=postgresql://username:password@localhost:5432/project_keystone
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=project_keystone

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: External Services
# Add any API keys for future integrations
# OPENAI_API_KEY=your_openai_api_key
# STRIPE_SECRET_KEY=your_stripe_secret_key